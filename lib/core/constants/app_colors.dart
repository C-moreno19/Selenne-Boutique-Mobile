import 'package:flutter/material.dart';

class AppColors {
  AppColors._();
  
  // Color principal
  static const Color primary = Color(0xFFD65391); // Rosa Selenne
  
  // Fondos
  static const Color background = Color(0xFFF5F5F5); // Gris claro
  static const Color inputBackground = Color(0xFFF3E5F5); // Rosa muy claro
  static const Color white = Color(0xFFFFFFFF);
  
  // Textos
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF666666);
  static const Color textHint = Color(0xFFB39DDB); // Morado claro
  
  // Bordes
  static const Color inputBorder = Color(0xFFE0E0E0);
  static const Color divider = Color(0xFFE0E0E0);
}