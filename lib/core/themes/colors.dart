import 'package:flutter/material.dart';

/// Colores de la marca Selenne Boutique
class AppColors {
  // Colores principales
  static const Color primary = Color(0xFFd65391); // Rose
  static const Color secondary = Color(0xFFf8a9c5); // Rose claro

  // Colores neutrales
  static const Color white = Color(0xFFffffff);
  static const Color lightGray = Color(0xFAf5f5f5);
  static const Color backgroundLight =
      Color(0xFAf5f5f5); // Same as lightGray for checkout/pages
  static const Color darkGray = Color(0xFF333333);
  static const Color black = Color(0xFF000000);

  // Colores funcionales
  static const Color success = Color(0xFF27AE60);
  static const Color warning = Color(0xFFF39C12);
  static const Color error = Color(0xFFE74C3C);
  static const Color info = Color(0xFF3498DB);

  // Colores de texto
  static const Color textPrimary = Color(0xFF333333);
  static const Color textSecondary = Color(0xFF808080);
  static const Color textLight = Color(0xFFB0B0B0);

  // Colores de borde
  static const Color borderLight = Color(0xFFE0E0E0);
  static const Color borderMedium = Color(0xFFD0D0D0);

  // Sombra
  static const Color shadow = Color(0x99000000);
}
